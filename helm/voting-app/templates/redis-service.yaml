apiVersion: v1
kind: Service

metadata:
  name: redis

spec:

  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: {{ .Release.Name }}

  ports:
    - port: 6379
      targetPort: redis

