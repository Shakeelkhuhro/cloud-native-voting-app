apiVersion: v1
kind: Secret

metadata:
  name: {{ include "result.fullname" . }}

type: Opaque

data:
  DB_USERNAME: {{ .Values.secret.username | b64enc }}
  DB_PASSWORD: {{ .Values.secret.password | b64enc }}
